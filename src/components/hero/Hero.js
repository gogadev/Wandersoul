import React from "react";

import "./hero.style.css";

const Hero = () => {
  return (
    <div className="hero">
      <div className="hero-text">~Change is the only constant in life~</div>
    </div>
  );
};

export default Hero;
